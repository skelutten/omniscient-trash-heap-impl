# Plan 99 — Future scope and exclusions

> **Status:** deferred; not active implementation work

This file contains capabilities that must not enter the core sequence without an explicit owner, dependency decision and acceptance gate.

- recurrence signals and DRIFT profiles;
- scale thresholds and production hardening claims;
- vector adapters and cross-encoder reranking;
- community detection and advanced graph retrieval;
- structural AST/LSP graph;
- Parquet/DuckDB staging when dependencies are unavailable;
- attested computation;
- `REL-009` until its proposed normative amendment is accepted;
- `E401–E499` until concrete OKF adapter failures exist.

Reversal condition: a future proposal must identify normative owner, runtime dependency, fixtures, tests, degraded behavior and a gate before becoming an active plan.

Source material: archived `FUTURE-TODO.md`, opt-in plans `90–93`, and `../specs/`.
